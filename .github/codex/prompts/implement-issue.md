# Implementar una issue

Trabaja en **pdf-fotos-a-imagenes** (`software`, `python`). Antes de editar, lee `AGENTS.md` y el contexto generado.

Contexto especializado: licencia=`mit`; git-init=`true`; github=`true`; github-visibility=`public`; publisher=`No aplica`; research-mode=`No aplica`; data-policy=`No aplica`; output=`No aplica`; plataforma=`No aplica`; automatización=`No aplica`; topología=`No aplica`; entorno=`No aplica`.

Implementa únicamente el alcance acordado. Añade pruebas, ejecuta `scripts/check.*` y los controles de seguridad aplicables. No publiques, no despliegues, no alteres secretos y no realices operaciones destructivas sin aprobación.
