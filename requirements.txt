# Las dependencias de ejecución se declaran en pyproject.toml.
# Para añadir, quitar o cambiar dependencias, edite [project].dependencies
# (o [project.optional-dependencies] para grupos opcionales) en pyproject.toml.
# Este fichero delega en esos metadatos para evitar mantener dos listas
# de versiones que puedan quedar desincronizadas.
.
