# Arquitectura

Describa componentes, límites, dependencias, interfaces, decisiones y trade-offs de **pdf-fotos-a-imagenes**.

Para el repositorio que mantenga este propio kit (`--kind software --language generic`), la fuente de verdad son los generadores. Los ejemplos deben regenerarse; no se corrigen manualmente sin corregir primero el generador.
